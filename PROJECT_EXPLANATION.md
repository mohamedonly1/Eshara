# دليل المهندس المبتدئ لفهم مشروع "إشارة" 🖐️✨

أهلاً بك يا صديقي البرمجي البطل! إذا كنت تبدأ رحلتك في عالم البرمجة والذكاء الاصطناعي، فهذا الدليل صُمم خصيصاً لك ليوضح لك تفاصيل هذا المشروع المميز خطوة بخطوة وبأسلوب ممتع وبسيط.

مشروع **"إشارة" (Ishara)** هو نظام متكامل وذكي جداً لترجمة لغة الإشارة العربية إلى نصوص مقروءة ونطقها، وجمع عينات البيانات ثلاثية الأبعاد لليد من المتطوعين، وتدريب نماذج ذكاء اصطناعي (AI Models) مخصصة لكل لغة إشارة بشكل آمن ودقيق.

---

## 🌟 الفكرة العامة للمشروع: كيف يعمل التطبيق؟

قبل الدخول في الأكواد، دعنا نفهم كيف تترجم الآلة إشارات اليد:
1. **الكاميرا** تلتقط صوراً متتالية ليدك (فيديو).
2. مكتبة **MediaPipe** (من شركة جوجل) تقوم بتحليل الصور وتكتشف يدك، ثم تحدد عليها **21 نقطة رئيسية** (المفاصل وأطراف الأصابع) بأبعاد ثلاثية (X, Y, Z).
3. نقوم بعملية **تطبيع للبيانات (Normalization)** لجعلها نسبية لمعصم اليد، وذلك حتى لا تتأثر الترجمة بمكان يدك في الشاشة أو بمدى قربك وبعدك عن الكاميرا.
4. نمرر هذه النقاط الـ 42 (x, y لكل نقطة من الـ 21) إلى **نموذج ذكاء اصطناعي (مستشعر عصبي)** قمنا بتدريبه مسبقاً.
5. النموذج يحلل العلاقات الرياضية والمسافات بين النقاط، ويتوقع الحرف العربي الذي تشير إليه يدك حالياً بنسبة احتمال معينة ويعرضه على الشاشة!

---

## 🛠️ التقنيات الأساسية المستخدمة (مستواك مبتدئ؟ لا تقلق!)

لقد استخدمنا مكتبات برمجية شهيرة وقوية جداً، وإليك دور كل منها ببساطة:
* **Python**: لغة البرمجة الأساسية التي كتبنا بها كل المنطق الخلفي وتدريب الموديل لسهولتها وقوتها.
* **OpenCV (cv2)**: مكتبة متخصصة في "رؤية الكمبيوتر" (Computer Vision). نستخدمها لفتح كاميرا الكمبيوتر، قراءة الصور، ورسم خطوط اليد والواجهات على الشاشة.
* **MediaPipe**: أداة سحرية مدعومة بالذكاء الاصطناعي تعثر على كف اليد وتستخرج إحداثيات مفاصل الأصابع الـ 21 في أجزاء من الثانية.
* **TensorFlow & Keras**: المكتبة العملاقة لبناء وتدريب الشبكات العصبية الاصطناعية (الذكاء الاصطناعي).
* **TensorFlow Lite (TFLite)**: نسخة مصغرة ومحسنة من الموديل تم تصميمها لتعمل بسرعة وخفة على المتصفحات والهواتف دون استهلاك بطارية أو موارد المعالج.
* **Flask**: إطار عمل بايثون لتطوير مواقع الويب. نستخدمه لتشغيل خادم الويب (Server) الذي يخدم صفحات الموقع وقاعدة البيانات للتحكم في المستخدمين.

---

## 📂 خريطة ملفات المشروع: ما هو دور كل ملف؟

مرفق في المشروع عدة ملفات بايثون، قمنا بتنظيمها وتوزيع المهام عليها ليكون الكود مرتباً وسهل الصيانة. إليك دور كل ملف بالتفصيل وبأسلوب بسيط:

### 1️⃣ مركز التهيئة والإعدادات: `config.py`
يحتوي هذا الملف على مسارات حفظ الملفات وتحديد المعاملات الافتراضية. ويحتوي على دالة `extract_derived_features` التي تقوم بعمليات حسابية هندسية متقدمة (مثل قياس الزوايا والمسافات بين المفاصل) لتعطي النموذج معلومات إضافية فائقة الدقة تساعده في التمييز بين الحروف المتشابهة.

### 2️⃣ أداة جمع البيانات: `collect_data.py`
برنامج محلي لجمع عينات التدريب عبر الكاميرا الموصلة بالجهاز وحفظها مباشرة في ملف الـ CSV.

### 3️⃣ مدرب الذكاء الاصطناعي: `train_model.py`
يأخذ البيانات التي جمعناها في ملف الـ CSV، ويقوم ببناء شبكة عصبية ذكية، ثم يبدأ بتدريبها عبر تكرار المحاولات وتعديل قيم الخلايا (الأوزان) تلقائياً لتقليل الخطأ ثم تصديرها كملف TFLite خفيف.

### 4️⃣ فاحص واختبار الموديل: `test_keypoints.py`
يقوم بتحميل الموديل الذي دربناه، ويقرأ ملف اختبار مستقل يحتوي على إشارات جديدة تماماً، ويمررها للموديل ليرى هل سيحزر الحرف الصحيح أم لا.

### 5️⃣ المقيم الخارجي: `evaluate_external.py`
يسمح لك من سطر الأوامر (Terminal) بتحديد أي موديل وأي ملف بيانات خارجي لتقييمهما بدقة وينتج تقريراً رقمياً بصيغة JSON ورسومات بيانية ملونة لمصفوفة الالتباس.

### 6️⃣ مسجل وضعيات اليد ثلاثية الأبعاد: `record_poses.py`
يقرأ إحداثيات يدك بالأبعاد الثلاثية الحقيقية (بما فيها العمق Z)، ويحفظ أفضل وضعية مثالية لكل حرف في ملف `poses.js` ويبث الحركة عبر بروتوكول الويب سوكيت (WebSocket).

### 7️⃣ مدير سجل الترجمات: `history.py`
يقوم بحفظ الكلمات المترجمة في ملف JSON خاص بالمستخدم ليعود إليها لاحقاً كسجل (History).

### 8️⃣ مدقق لغات الموقع: `validate_translations.py`
بفحص ملفات الأكواد البرمجية للواجهات (HTML Templates) ويقارنها بملفات الترجمة للتأكد من تطابق كافة الكلمات المترجمة في اللغات المدعومة.

### 9️⃣ نظام إدارة الحسابات: `auth.py`
يتحكم في تسجيل حسابات جديدة، تسجيل الدخول، التحقق من البريد الإلكتروني بإرسال رمز تحقق مؤقت (OTP)، وتشفير كلمات المرور بشكل آمن للغاية.

### 🔟 خادم الويب والقلب النابض: `server.py`
تطبيق الويب الأساسي (Flask Web Server) الذي يستقبل طلبات المستخدمين من المتصفحات، ويعرض واجهات الموقع، ويقوم بتشغيل نموذج الذكاء الاصطناعي للترجمة.

---

## 🌐 هيكل واجهات الويب (Templates) وكيف تعمل الواجهة الأمامية؟

يتكون الجزء المرئي للمستخدم (Front-end) من صفحات ويب تفاعلية قمنا بكتابتها باستخدام **HTML** للهيكل، و**CSS** للتصميم الأنيق، ولغة **JavaScript** للتفاعل والتحريك. نستخدم في خادم بايثون نظام **Jinja2 Templates**، وهو نظام يسمح لنا بدمج أكواد بايثون داخل صفحات الويب (مثل عرض اسم المستخدم الحالي، أو التحقق من صلاحيات المدير، أو استدعاء ملفات الترجمة المتعددة باستخدام الدالة `{{ t('key') }}`).

إليك تفصيل لأهم واجهات الويب في مجلد `templates/` ودورها التقني:

### 1️⃣ الصفحة الرئيسية للترجمة المباشرة: `index.html`
* **دورها**: فتح الكاميرا والتعرف على الإشارات في الوقت الفعلي.
* **آلية عملها البرمجية**:
  1. **الكاميرا والكانفاس**: نستخدم كود JavaScript للوصول لكاميرا الويب الخاصة بالمستخدم، ونعرض بث الفيديو داخل وسم `<video>`. ونضع فوق الفيديو لوحة رسم شفافة `<canvas>` لنرسم عليها خطوط اليد الملونة.
  2. **مكتبة MediaPipe بالمتصفح**: نقوم بجلب مكتبة MediaPipe لليد عبر الإنترنت لتتبع اليد مباشرة في المتصفح. ترسل المكتبة نقاط اليد المكتشفة لدالة الرسم ودالة التنبؤ.
  3. **إرسال الإحداثيات (API Fetch)**: نأخذ النقاط الـ 42 ونقوم بإرسالها في الخلفية إلى خادم بايثون عبر طلب شبكة `fetch('/predict')` كل 120 ملي ثانية (حوالي 8 مرات في الثانية) للحصول على الحرف المتوقع.
  4. **خوارزمية الفلترة ومنع الارتعاش**: لتجنب الأخطاء، قمنا بكتابة خوارزمية ذكية تحتفظ بآخر 10 توقعات متتالية. لا نكتب الحرف على الشاشة إلا إذا اتفقت التوقعات بنسبة تفوق 75% ولم يتكرر الحرف مرتين متتاليتين بسرعة.
  5. **نطق الكلمات (Text-to-Speech)**: نستخدم ميزة المتصفح المضمنة `window.speechSynthesis` لنطق الكلمات المترجمة صوتياً عند الضغط على زر المكبر لمساعدة الصم والبكم.

### 2️⃣ واجهة المحاكاة والترجمة العكسية ثلاثية الأبعاد: `translate.html`
* **دورها**: أخذ نص مكتوب من المستخدم وتحويله إلى حركة لغة إشارة حية يمثلها مجسم يد ثلاثي أبعاد.
* **آلية عملها البرمجية**:
  1. **محرك الرسوميات Three.js (WebGL)**: نستخدم مكتبة Three.js الشهيرة لبناء بيئة ثلاثية الأبعاد تفاعلية تحتوي على كاميرا افتراضية وإضاءات وظلال.
  2. **تحميل مجسم اليد (GLTFLoader)**: نقوم بتحميل نموذج يد مفصل حقيقي بصيغة `hand.glb` يحتوي على مفاصل عظام وهيكل كامل (Rigged Skeleton).
  3. **محرك الحسابات العكسية للحركة (Inverse Kinematics)**: تأخذ دالة `getTargetLocalRotations` إحداثيات الحرف ثلاثية الأبعاد من ملف `poses.js` (المسجل بواسطة `record_poses.py`)، وتقوم بعمليات رياضية معقدة لحساب زوايا الدوران (Quaternions) المناسبة لكل عظمة ومفصل لتطابق شكل الحرف المطلوب تماماً.
  4. **حركات انتقالية ناعمة (Slerp Interpolation)**: نستخدم الدوران الكروي المتناسق (slerp) للانتقال بسلاسة بين الحروف، مما يعطي حركة يد واقعية تشبه حركة اليد البشرية دون قفزات مفاجئة.
  5. **التعرف على الصوت (Speech-to-Text)**: تحتوي الصفحة على ميزة التعرف على الصوت باستخدام `SpeechRecognition` ليتمكن المتكلم من نطق الكلمة بصوته فتتحول إلى نص مكتوب ثم تترجمها اليد ثلاثية الأبعاد فوراً.

### 3️⃣ واجهة تسجيل الحسابات والملف الشخصي والتحقق:
- **`login.html`**:
  تسمح للمستخدمين الجدد بإنشاء حساباتهم، وللمستخدمين الحاليين بتسجيل الدخول.
  ```javascript
  // كود التبديل بين واجهة تسجيل الدخول وإنشاء حساب جديد
  function switchTab(tab) {
      document.getElementById('loginForm').style.display = tab === 'login' ? 'block' : 'none';
      document.getElementById('registerForm').style.display = tab === 'register' ? 'block' : 'none';
  }
  ```
- **`verify_email.html`**:
  تقوم بالتحقق من البريد الإلكتروني للمستخدمين الجدد عبر رمز الـ OTP المكون من 6 أرقام.
  ```javascript
  // كود ينقل مؤشر الكتابة تلقائياً إلى المربع التالي عند كتابة الرقم
  inputs.forEach((input, idx) => {
      input.addEventListener('input', (e) => {
          if (e.target.value.length === 1 && idx < inputs.length - 1) {
              inputs[idx + 1].focus(); // التركيز على المربع التالي
          }
      });
  });
  ```
- **`profile.html`**:
  تتيح للمستخدمين تعديل أسمائهم، ورفع صورهم الشخصية، ومتابعة عينات الحروف التي ساهموا بجمعها في النظام. كما توضح له النسبة المئوية لإنجاز كل حرف (من أصل 200 عينة مطلوبة) بشكل مرئي جميل وجذاب.
  ```javascript
  // كود حساب نسبة إنجاز الحرف وعرض الشريط الملون
  const pct = Math.min(100, Math.round((c / 200) * 100));
  const barColor = pct >= 100 ? 'var(--accent)' : (pct >= 50 ? 'var(--warn)' : 'var(--text-dim)');
  ```

### 4️⃣ صفحة محرر وضعيات اليد التفاعلي: `pose_editor.html`
* **دورها**: تتيح للمشرفين والمهندسين ضبط وضعيات اليد ثلاثية الأبعاد لكل حرف إشارة بشكل تفاعلي ومباشر.
* **آلية عملها البرمجية**:
  - تقوم بتحميل مجسم اليد عبر مكتبة Three.js.
  - عرض لوحة تحكم تحتوي على أشرطة تمرير (Sliders) للتحكم بزوايا الدوران (X, Y, Z) لكل عظمة من عظام مفاصل اليد البالغ عددها 20 مفصلاً.
  - تمكن المشرف من حفظ الوضعية مباشرة على السيرفر لتحديث ملف `poses.js` دون الحاجة لكتابة إحداثيات رياضية يدوياً.

### 5️⃣ لوحة تحكم المشرف والمدير: `admin.html`
* **دورها**: لوحة مراقبة شاملة للنظام مخصصة للمشرفين لمتابعة الأداء وإدارة المساهمين واللغات.
* **آلية عملها البرمجية**:
  - **بوابة الحماية (Verification Gate)**: تطلب إدخال كلمة مرور الإدارة وتتحقق منها برمجياً.
  - **جدول اللغات والذكاء الاصطناعي**: يعرض اللغات المدعومة وحالة ملفات الموديل (هل هو مدرب؟ هل الحجم متناسق؟ هل هناك مشكلة ترميز؟).
  - **إدارة الحسابات**: تسمح بتفعيل أو تعطيل حسابات المساهمين، ترقية رتبهم إلى مشرفين، أو حذف حساباتهم نهائياً.
  - **تصدير التقارير**: توفر روابط سريعة لتحميل بيانات الإحصائيات بالكامل بصيغة JSON أو CSV.

---

## 🧠 الكود تحت المجهر: كيف نلتقط ونحلل الميزات الرياضية لليد؟

أهم ما يميز مشروع "إشارة" هو كيف يُسهل على الذكاء الاصطناعي فهم الإشارات المعقدة. دعنا نلقي نظرة على الكود المسؤول عن حساب الميزات الهندسية المشتقة من ملف `config.py`:

```python
# كود حساب المسافة والزوايا بين مفاصل الأصابع الخمسة
def extract_derived_features(coords):
    if len(coords) != 42:
        return [0.0] * 62 # في حال وجود خطأ في البيانات نرجع أصفاراً
        
    points = [(coords[i], coords[i+1]) for i in range(0, 42, 2)]
    
    # حساب المسافة الإقليدية بين نقطتين
    def dist(i, j):
        return math.sqrt((points[i][0] - points[j][0])**2 + (points[i][1] - points[j][1])**2)
        
    # حساب المتجه بين نقطتين
    def vec(i, j):
        return (points[j][0] - points[i][0], points[j][1] - points[i][1])
        
    # حساب جيب تمام الزاوية (Cosine Similarity) لمعرفة اتجاه الأصابع
    def angle_cos(v1, v2):
        n1 = math.sqrt(v1[0]**2 + v1[1]**2)
        n2 = math.sqrt(v2[0]**2 + v2[1]**2)
        if n1 == 0 or n2 == 0:
            return 0.0
        return max(-1.0, min(1.0, (v1[0] * v2[0] + v1[1] * v2[1]) / (n1 * n2)))
```

### 🔒 نظام الحماية التلقائي ضد هجمات الـ CSRF (أمن المعلومات)
لحماية الموقع من هجمات الاختراق الخارجية (Cross-Site Request Forgery)، قمنا بتطبيق آلية تدرج رمز أمان مشفر مؤقت (CSRF Token) في طلبات المتصفح. 

إليك كيف يتم ذلك تلقائياً في الواجهة الأمامية بـ JavaScript:
```javascript
(function() {
    // دالة لاستخراج توكن الحماية من الكوكي
    function getCsrfToken() {
        const match = document.cookie.match(/(?:^|;\s*)csrf_token=([^;]*)/);
        return match ? decodeURIComponent(match[1]) : '';
    }
    
    // حفظ دالة Fetch الافتراضية
    const _originalFetch = window.fetch;
    
    // تغليف دالة Fetch لإضافة التوكن تلقائياً لكل طلب كتابة أو تعديل بيانات
    window.fetch = function(url, options) {
        options = options || {};
        const method = (options.method || 'GET').toUpperCase();
        if (method !== 'GET' && method !== 'HEAD') {
            options.headers = options.headers || {};
            options.headers['X-CSRFToken'] = getCsrfToken(); // إرفاق توكن الحماية
        }
        return _originalFetch.call(this, url, options);
    };
})();
```

---

## 🎯 نصائح لك يا بطل لبدء العمل والتطوير:

1. **ابدأ بقراءة ملف `config.py`**: لتفهم كيف تُحسب الميزات المشتقة لليد رياضياً.
2. **شغّل ملف `collect_data.py`**: جرب جمع بيانات لنفسك لترى كيف يتعرف MediaPipe على يدك ويرسم الهيكل الملون.
3. **راجع ملف `test_keypoints.py`**: وحاول قراءة الكود لمعرفة كيف نقوم بتحميل نموذج TFLite وتمرير البيانات له وتلقي التوقعات.
4. **لا تخف من الأخطاء!**: البرمجة مبنية على التجربة والخطأ. إذا ظهر لك خطأ في الـ Terminal، اقرأ رسالة الخطأ جيداً، فغالباً ستخبرك بالسطر والملف المسبب للمشكلة.

أتمنى لك رحلة ممتعة في تطوير وإدارة مشروع "إشارة"! أنت الآن تمتلك كوداً برمجياً غنياً بالتعليقات التوضيحية المفصلة والمكتوبة بأسلوب بشري سهل وقريب للقلب. 🚀💻
